"use client";
import React, { useState } from 'react';
// Importando apenas o necessário para não dar erro
import { ChevronLeft, Check, Target } from 'lucide-react';

export default function HabitsPage() {
  const [habits, setHabits] = useState([
    { id: 1, name: "Treino na Academia", done: false },
    { id: 2, name: "Leitura Técnica", done: false },
  ]);

  return (
    <div style={{ backgroundColor: '#09090b', minHeight: '100vh', color: 'white', padding: '20px' }}>
      {/* Botão de Voltar */}
      <button 
        onClick={() => window.location.href = '/'}
        style={{ background: '#18181b', border: '1px solid #27272a', color: 'white', padding: '10px', borderRadius: '10px', marginBottom: '20px' }}
      >
        <ChevronLeft size={20} />
      </button>

      <h1 style={{ fontSize: '24px', fontWeight: 'bold', marginBottom: '20px' }}>Meus Hábitos</h1>

      <div style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}>
        {habits.map(habit => (
          <div 
            key={habit.id}
            style={{ backgroundColor: '#18181b', padding: '20px', borderRadius: '15px', border: '1px solid #27272a', display: 'flex', justifyContent: 'space-between' }}
          >
            <span>{habit.name}</span>
            <Target size={20} color="#a855f7" />
          </div>
        ))}
      </div>
      
      <p style={{ marginTop: '30px', color: '#71717a', fontSize: '12px', textAlign: 'center' }}>
        Jarvis: "Mantenha o foco, Rogério!"
      </p>
    </div>
  );
}
